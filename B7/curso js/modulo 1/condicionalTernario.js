// vulgo if de uma linha só

// sendo
// ? = if
// : = else

let membro = true

// aqui vemos um exemplo
let frete = membro ? 2 : 10
// lê-se: membro, se for true valor é 2, se n fica 10

// pode ser usado entre parenteses
// let frete = (membro ? 2 : 10)